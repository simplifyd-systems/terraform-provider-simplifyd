---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "simplifyd_project Resource - simplifyd"
subcategory: ""
description: |-
  A project groups environments under a workspace.
---

# simplifyd_project (Resource)

A project groups environments under a workspace.

## Example Usage

```terraform
resource "simplifyd_project" "storefront" {
  name = "storefront"
}

# Projects are created with one environment already in place. Manage additional
# ones with simplifyd_environment.
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Human-readable project name.

### Optional

- `workspace` (String) Workspace slug. Defaults to the provider's `workspace`.

### Read-Only

- `id` (String) `<workspace>/<project>`.
- `network_slug` (String) Slug of the private network backing this project.
- `slug` (String) Server-assigned slug, derived from the name at creation.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Projects are identified by <workspace>/<project>.
terraform import simplifyd_project.storefront acme/storefront
```
