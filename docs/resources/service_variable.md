---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "simplifyd_service_variable Resource - simplifyd"
subcategory: ""
description: |-
  An environment variable set on a service.
  Values are stored in Terraform state. Use a secret store (Vault, SSM, ephemeral resources) as the source rather than literals in .tf files.
---

# simplifyd_service_variable (Resource)

An environment variable set on a service.

Values are stored in Terraform state. Use a secret store (Vault, SSM, `ephemeral` resources) as the source rather than literals in `.tf` files.

## Example Usage

```terraform
resource "simplifyd_service_variable" "database_url" {
  service = simplifyd_service.api.slug
  name    = "DATABASE_URL"
  value   = "postgres://app@${simplifyd_service.db.private_hostname}:5432/orders"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Variable name. Changing it forces replacement.
- `service` (String) Slug of the service the variable belongs to.
- `value` (String, Sensitive) Variable value.

### Optional

- `env` (String) Environment slug. Defaults to the provider's `env`.
- `project` (String) Project slug. Defaults to the provider's `project`.
- `workspace` (String) Workspace slug. Defaults to the provider's `workspace`.

### Read-Only

- `id` (String) `<workspace>/<project>/<env>/<service>/<variable>`.
- `slug` (String) Server-assigned slug.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Variables are identified by <workspace>/<project>/<env>/<service>/<variable>.
terraform import simplifyd_service_variable.database_url acme/storefront/production/api/DATABASE_URL
```
